# منصة «احتياج» — الوصف الرئيسي والمرجع المعتمد للمشروع (نسخة معدّلة)

> **ملخص أهم الإضافات في هذه النسخة:**
> 1. آلية واضحة لربط الاحتياج بالجمعية المناسبة (نطاق تغطية جغرافي وتصنيفي).
> 2. تحديد من يضع الأولوية للاحتياج ومتى.
> 3. إشعار المستفيد عند تغيّر حالة احتياجه.
> 4. توضيح العلاقة بين "عدم تعديل الاحتياج" وحذف الحساب (Anonymize بدل Delete الفعلي).
> 5. سجل تدقيق (Audit Log) لأي إجراء إداري على السجلات.
> 6. موافقة صريحة على سياسة الخصوصية عند تسجيل المستفيد (تمهيدًا للامتثال لنظام حماية البيانات الشخصية السعودي PDPL).
> 7. جدول مرجعي ثابت للمناطق والمدن (Reference Data) منذ اليوم الأول.
> 8. توضيح حالة رفض الجمعية عند التقديم.
>
> كل الإضافات مكتوبة كافتراضات معقولة للـ MVP، ويمكن تعديلها. الأقسام الأصلية بقيت كما هي إلا حيث أُشير لتعديل.

---

## 1. الفكرة الجوهرية

جوهر «احتياج» هو:

جمع الاحتياج من المستفيد نفسه، ثم تحويل الاحتياجات إلى بيانات منظمة ومجمعة يمكن تحليلها وفهم توزيعها الجغرافي، ومن ثم مساعدة الجمعيات والجهات على بناء مبادرات ومشاريع مبنية على الاحتياج الفعلي.

**التدفق الأساسي:**

المستفيد → تسجيل الاحتياج → تنظيم وتصنيف → تحليل وتجميع → خريطة الاحتياج → اكتشاف الأولويات → المبادرات والمشاريع → الأثر

الخريطة ليست مجرد خريطة، بل واجهة لعرض قاعدة بيانات الاحتياج بشكل جغرافي.

---

## 2. الأطراف في المنصة

1. المستفيد
2. الجمعية
3. الجهة المانحة (مستقبلًا)
4. المتطوع (مستقبلًا)
5. إدارة المنصة
6. الموقع العام

الإصدار الأولي لا يحتوي على حساب للجهات المانحة.

---

## 3. الإصدار الأولي MVP

يركز على: الموقع العام، حساب المستفيد، حساب الجمعية، لوحة إدارة أساسية.

لا يُبنى في الإصدار الأول:
- حساب الجهة المانحة
- نظام المتطوع الكامل
- الدفع الإلكتروني
- تطبيقات الجوال
- الذكاء الاصطناعي المتقدم
- أنظمة متقدمة غير ضرورية للـ MVP

---

## 4. المستفيد Beneficiary

المستفيد هو نقطة البداية في المنصة، ويقوم بإنشاء حساب بنفسه. الجمعيات لا تستطيع إنشاء حسابات للمستفيدين نيابة عنهم.

### 🆕 4.1 الموافقة على سياسة الخصوصية (خطوة إلزامية)

عند التسجيل، يجب أن يمر المستفيد بخطوة موافقة صريحة (Explicit Consent) قبل إكمال الحساب، توضح:
- ما البيانات التي تُجمع
- من يمكنه رؤية أي جزء منها (الجمعية ترى فقط الاسم/التواصل/الاحتياج)
- أن البيانات الصحية تُعامل كبيانات حساسة

هذه الخطوة لا يمكن تجاوزها، وتُسجَّل (تاريخ ووقت الموافقة) لأغراض الامتثال لنظام حماية البيانات الشخصية (PDPL).

### 🆕 4.2 التحقق من الهوية (افتراضي MVP)

في الإصدار الأول: تسجيل عبر رقم الجوال + رمز تحقق (OTP)، بدون ربط بنفاذ. لا يُسمح بإنشاء أكثر من حساب لنفس رقم الجوال. ربط نفاذ/الهوية الوطنية يبقى تحسينًا مستقبليًا لتقليل التكرار والانتحال، ويُذكر كملاحظة تصميمية حتى لا يحتاج النظام إعادة بناء لاحقًا.

---

## 5. بيانات المستفيد

بعد إنشاء الحساب يكمل المستفيد البيانات المطلوبة. يجب التعامل مع البيانات الشخصية والصحية على أنها بيانات حساسة (راجع قسم 39 المعدّل).

---

## 6. تسجيل الاحتياج

نموذج احتياج رقمي منظم، بنوع رئيسي وتصنيفات فرعية (كما في الوصف الأصلي: أجهزة ومستلزمات طبية، علاج وتأهيل، فحوصات وكشف مبكر، رعاية صحية منزلية، دعم نفسي، توعية ووقاية، أخرى).

### 🆕 6.1 المرفقات (مؤجَّلة صراحة في MVP)

رفع مستندات أو تقارير طبية داعمة للاحتياج **غير موجود في الإصدار الأول**. النموذج نصي/اختياري فقط. يُذكر هذا صراحة حتى لا يُفترض ضمنيًا أثناء التطوير، مع ترك حقل `attachments` فارغًا في تصميم قاعدة البيانات تحسبًا للمستقبل.

---

## 7. قاعدة عدم تعديل الاحتياج السابق

بعد إرسال الاحتياج، لا يستطيع المستفيد تعديله. أي تغيّر في الحالة أو ظهور احتياج جديد يعني تسجيل احتياج جديد، وليس تعديل السجل القديم. الهدف: دقة البيانات، تاريخ الاحتياجات، سلامة التحليلات.

سجلات الاحتياجات **Immutable** بعد الإرسال من جهة المستفيد. أي تصحيح يتم فقط عبر صلاحيات إدارية محددة، وليس من حساب المستفيد.

### 🆕 7.1 العلاقة بين Immutability وحذف الحساب

إذا طلب المستفيد حذف حسابه (حق قد يكفله PDPL)، فإن سجلات احتياجاته **لا تُحذف فعليًا** لأنها ضرورية لسلامة التحليلات التاريخية. بدلًا من ذلك:
- يُحذف الحساب وبياناته التعريفية (الاسم، وسيلة التواصل).
- تبقى سجلات الاحتياج في قاعدة البيانات لكن بحالة **Anonymized** (مرجع مجهول بدل مرجع مستفيد فعلي)، بحيث تستمر ضمن الإحصائيات والخريطة دون إمكانية ربطها بشخص.

هذه قاعدة تصميمية أساسية يجب أن تنعكس في مخطط قاعدة البيانات (لا Foreign Key صارم يمنع الحذف، بل علاقة تسمح بالـ Soft Delete/Anonymize).

---

## 8. حالة الاحتياج والإشعارات

الحالات: جديد، قيد المراجعة، معتمد، قيد المعالجة، تمت الاستجابة، مغلق.

المستفيد يشاهد حالة احتياجه لكن لا يعدّله.

### 🆕 8.1 إشعار المستفيد عند تغيّر الحالة

عند تغيّر حالة أي احتياج، يجب إشعار المستفيد. في MVP: إشعار داخل التطبيق (In-app Notification) عند الدخول + رسالة نصية SMS اختيارية إن توفر مزود رسائل. لا حاجة لبناء نظام إشعارات معقد (Push/Email) في هذه المرحلة، لكن يُبنى الجدول (`notifications`) بشكل يسمح بالتوسعة لاحقًا لقنوات إضافية.

---

## 9. تعدد الاحتياجات

المستفيد يستطيع تسجيل أكثر من احتياج، وكل احتياج سجل مستقل.

---

## 🆕 10. آلية تحديد الأولوية

الأولوية (منخفضة/متوسطة/عالية) **لا تُحسب تلقائيًا بخوارزمية في MVP**. تُحدَّد يدويًا من قِبل الإدارة أو الجمعية المختصة أثناء مرحلة "قيد المراجعة"، بناءً على معايير نوعية بسيطة (مثال: نوع الاحتياج + ملاحظة نصية اختيارية من المراجع). حساب الأولوية آليًا (بناءً على نوع الحالة الصحية، تكرار الاحتياج، إلخ) يبقى تحسينًا مستقبليًا (Phase 6 أو ما بعده).

---

## 🆕 11. آلية ربط الاحتياج بالجمعية (Matching Logic)

هذه نقطة كانت غير محددة في الوصف الأصلي وهي ضرورية لتشغيل لوحة الجمعية فعليًا:

- كل جمعية تُعرّف عند الاعتماد **نطاق تغطية (Coverage Scope)**: منطقة/مدينة + نوع أو أكثر من أنواع الاحتياج التي تعمل عليها. يُدخل هذا من قبل الإدارة أو الجمعية نفسها عند التسجيل، ويخضع لمراجعة الإدارة.
- الاحتياج الذي يقع ضمن نطاق تغطية جمعية معينة (جغرافيًا + تصنيفيًا) **يظهر في لوحتها** ضمن "احتياجات متاحة لك".
- إذا تقاطع احتياج مع أكثر من جمعية، يظهر لجميعها (مبدئيًا)، وأي جمعية تبدأ المعالجة تُحدّث الحالة إلى "قيد المعالجة" (أول جمعية تتحرك "تحجز" الاحتياج ضمن النظام لتفادي ازدواج الجهود؛ تفاصيل هذا الحجز تحتاج قرار عمل — انظر الملاحظة أدناه).
- لا يوجد تعيين يدوي معقد من الإدارة لكل احتياج في MVP؛ المطابقة تلقائية بناءً على نطاق التغطية فقط.

> **ملاحظة تحتاج قراركم:** هل يُسمح لأكثر من جمعية بالعمل على نفس الاحتياج بالتوازي، أم يجب "حجزه" لجمعية واحدة بمجرد بدء المعالجة؟ الافتراض أعلاه (أول من يتحرك يحجز) هو الأبسط للـ MVP، ويمكن تغييره.

---

## 12. الجمعية Association

الجمعية تنشئ حسابًا يمر بمرحلة تحقق واعتماد من الإدارة. بعد الاعتماد تحصل على لوحة التحكم.

### 🆕 12.1 حالة الرفض

إذا رفضت الإدارة طلب الجمعية، يجب أن تصل رسالة توضيحية للجمعية (سبب الرفض إن أمكن، أو رسالة عامة) مع إمكانية إعادة التقديم بعد تحديث بياناتها. لا يُترك الطلب "معلقًا" دون رد.

---

## 13. ماذا ترى الجمعية؟

فقط: الاسم، وسيلة التواصل، الاحتياج، الأولوية — للاحتياجات ضمن نطاق تغطيتها فقط (راجع قسم 11).

## 14. ماذا لا تستطيع الجمعية رؤيته؟

الملف الشخصي الكامل، البيانات الصحية التفصيلية، الوثائق الشخصية، أي معلومة خارج نطاق الصلاحية. مبدأ **Least Privilege**.

---

## 15. لوحة الجمعية

إحصائيات: عدد الاحتياجات، الجديدة، حسب النوع، حسب الأولوية، حسب المنطقة/المدينة، حسب الحالة — لكن **مقتصرة على نطاق تغطية الجمعية نفسها**، لا كل بيانات المنصة.

---

## 16. خريطة الاحتياج للجمعية

تعرض بيانات مجمعة ضمن نطاق صلاحيات الجمعية، بفلاتر: المنطقة، المدينة، النوع، التصنيف، الأولوية، الحالة، الفترة الزمنية.

---

## 17. إنشاء المبادرات

الجمعية تبني مبادرات بناءً على بيانات الاحتياج المتاحة لها (الاحتياج → البيانات → اكتشاف المشكلة → المبادرة).

---

## 18. الجهة المانحة Donor (مستقبلًا فقط)

لا يوجد حساب للجهة المانحة في الإصدار الأول: لا تسجيل، لا Login، لا Dashboard، لا Payment Gateway.

عند بنائها مستقبلًا: لا ترى أي بيانات تعريفية للمستفيد، فقط الاحتياج والبيانات المجمعة والتحليلات، ويمكنها لاحقًا استعراض المبادرات واختيار دعمها.

---

## 19. قاعدة الخصوصية الأساسية

| الطرف | يرى |
|---|---|
| الجمعية | الاسم + وسيلة التواصل + الاحتياج (ضمن نطاق تغطيتها) |
| الجهة المانحة (مستقبلًا) | الاحتياج + البيانات المجمعة فقط |
| الموقع العام | بيانات مجمعة وإحصائيات فقط |

---

## 20. خريطة المملكة العربية السعودية

الخريطة تصميميًا تدعم جميع مناطق المملكة، لكن البيانات المعروضة فعليًا في الإصدار الأول تخص **منطقة الحدود الشمالية فقط**. باقي المناطق تظهر بصريًا بدون بيانات فعلية أو أرقام موحية.

### 🆕 20.1 جدول مرجعي ثابت للمناطق والمدن (Reference Data)

يجب بناء جدول `Regions` وجدول `Cities` (مرتبط بمنطقته) يغطي **كل مناطق ومدن المملكة الـ13** منذ اليوم الأول، حتى لو لم تحتوِ حاليًا على بيانات احتياج. هذا يمنع الحاجة لإعادة بناء قاعدة البيانات أو منطق الخريطة عند توسيع النطاق الجغرافي لاحقًا لمناطق أخرى غير الحدود الشمالية. البيانات الفعلية (سجلات الاحتياج) تُربط فقط بمدن منطقة الحدود الشمالية حاليًا (عرعر، رفحاء، طريف، العويقيلة، وغيرها).

---

## 21. طريقة عرض الخريطة

Saudi Arabia Map كاملة بصريًا، مع "Northern Borders Region = Active Data Region"، وباقي المناطق بحالة "قريبًا" أو بدون بيانات.

---

## 22. البيانات التجريبية

Mock Data واقعية الشكل، قابلة للاستبدال بقاعدة بيانات حقيقية، وواضح داخل المشروع أنها تجريبية.

---

## 23. الموقع العام

الرئيسية، عن احتياج، الجمعيات، المبادرات، التطوع، خريطة الاحتياج، تسجيل الدخول، الانضمام.

---

## 24. قاعدة بيانات الاحتياج (Data Model)

الاحتياج كيان مستقل، لا حقل داخل ملف المستفيد:

```
Need
├── ID
├── Beneficiary Reference (قابل للـ Anonymize، راجع 7.1)
├── Category
├── Subcategory
├── Region (مرجع لجدول Regions)
├── City (مرجع لجدول Cities)
├── Priority (تُحدَّد يدويًا، راجع قسم 10)
├── Status
├── Association (مرجع اختياري — الجمعية التي تعالج الاحتياج حاليًا، راجع قسم 11)
├── Created At
└── Updated At
```

التجميع: Region → City → Need Category → Priority → Count.

---

## 25. عدم تعديل الاحتياج

لا زر Edit Need. فقط Add New Need.

---

## 26. إدارة المنصة Admin

الإدارة تدير: المستفيدين، الجمعيات (اعتماد/رفض)، تصنيفات الاحتياج، الحالات، مراقبة الاحتياجات، المحتوى، المبادرات، الصلاحيات، الإحصائيات، وتصحيح السجلات وفق صلاحيات محددة.

### 🆕 26.1 سجل تدقيق (Audit Log)

كل إجراء إداري يعدّل بيانات حساسة أو سجل احتياج (تصحيح، تغيير حالة، اعتماد/رفض جمعية) يجب أن يُسجَّل في جدول `AuditLog` يحتوي: من نفّذ الإجراء، ماذا تغيّر، وقت التنفيذ. هذا ضروري للمساءلة نظرًا لحساسية البيانات، ويجب أن يكون جزءًا من التصميم الأساسي وليس إضافة لاحقة.

---

## 27. الصلاحيات RBAC

الإصدار الأول: `ADMIN`, `BENEFICIARY`, `ASSOCIATION`. لاحقًا: `DONOR`, `VOLUNTEER`.

---

## 28. الخصوصية والأمان (معدّل)

Privacy by Design و Security by Design، مع مراعاة الأنظمة السعودية ذات العلاقة — وتحديدًا **نظام حماية البيانات الشخصية (PDPL)** الصادر عن SDAIA، باعتباره المرجع النظامي المباشر لمنصة تجمع بيانات صحية وشخصية حساسة.

نقاط تنفيذية إضافية:
- تشفير البيانات الصحية الحساسة عند التخزين (Encryption at Rest) وليس فقط أثناء النقل (HTTPS).
- الاحتفاظ بموافقة المستفيد الصريحة (راجع 4.1) كسجل قابل للإثبات.
- سياسة احتفاظ/حذف واضحة (راجع 7.1).
- سجل تدقيق لكل وصول أو تعديل إداري (راجع 26.1).

المبدأ الأساسي يبقى: لا يتم عرض أي معلومة للمستخدم إلا إذا كانت ضرورية لدوره وصلاحياته (Least Privilege).

---

## 29. التقنية

**Frontend:** Next.js, React, TypeScript, Tailwind CSS
**Backend:** NestJS, TypeScript
**Database:** PostgreSQL
**ORM:** Prisma
**Map:** MapLibre GL JS (أو حل مناسب)
**Charts:** Recharts أو ECharts
**Version Control:** Git + GitHub

---

## 30. Architecture (المراحل)

```
Phase 1 → Public Website
Phase 2 → Beneficiary
Phase 3 → Association
Phase 4 → Donor
Phase 5 → Volunteer
Phase 6 → Advanced Admin (+ Audit Log dashboards, Priority algorithm)
```

---

## 31. أهم قواعد المشروع (محدّثة)

1. المستفيد يسجل نفسه.
2. المستفيد لا يستطيع تعديل احتياج سابق بعد إرساله.
3. المستفيد يستطيع إنشاء احتياج جديد.
4. الجمعية لا ترى الملف الكامل للمستفيد.
5. الجمعية ترى فقط الاسم + وسيلة التواصل + الاحتياج، بالقدر اللازم وضمن نطاق تغطيتها.
6. الجهة المانحة لا ترى أي بيانات تعريفية للمستفيد.
7. لا يوجد حساب للجهة المانحة في الإصدار الأول.
8. الخريطة كاملة للمملكة (13 منطقة كمرجع بيانات، راجع 20.1).
9. البيانات المعروضة فعليًا في الإصدار الأول تخص منطقة الحدود الشمالية فقط.
10. الموقع العام يعرض بيانات مجمعة فقط.
11. لا تُستخدم بيانات حقيقية في الـ MVP.
12. لا تُبنى Features مستقبلية لم يتم طلبها.
13. 🆕 عند تغيّر حالة الاحتياج، يُشعَر المستفيد.
14. 🆕 لا يُحذف سجل احتياج فعليًا أبدًا؛ عند حذف الحساب يُجهَّل السجل (Anonymize) لا يُحذف.
15. 🆕 كل جمعية لها نطاق تغطية (منطقة/مدينة + نوع احتياج) يحدد الاحتياجات التي تظهر لها.
16. 🆕 كل إجراء إداري على بيانات حساسة يُسجَّل في Audit Log.
17. 🆕 لا يكتمل تسجيل المستفيد دون موافقة صريحة على سياسة الخصوصية.

---

## 32. الرؤية النهائية

منظومة تساعد على الإجابة عن: أين يوجد الاحتياج؟ ما نوعه؟ كم حجمه؟ ما أولويته؟ كيف يتوزع جغرافيًا؟ ما المبادرات التي يمكن أن تستجيب له؟ ليصبح القرار مبنيًا على بيانات الاحتياج الفعلي.

---

## 33. الجملة المختصرة للمشروع

«احتياج» منصة رقمية تجمع احتياجات المستفيدين وتحولها إلى بيانات منظمة وقابلة للتحليل والعرض الجغرافي، لمساعدة الجمعيات والجهات ذات العلاقة على فهم الاحتياج الحقيقي وبناء مبادرات أكثر أثرًا.

---

## 34. قاعدة التطوير الأخيرة

فرّق دائمًا بين ما هو موجود في الإصدار الحالي وما هو مخطط للمستقبل. الإصدار الأول يركز على: الموقع العام + المستفيد + الجمعية + خريطة احتياج الحدود الشمالية + البيانات والتحليلات الأساسية + الإضافات في هذه النسخة (نطاق تغطية الجمعية، الأولوية اليدوية، الإشعارات الأساسية، Audit Log، الموافقة على الخصوصية، الجدول المرجعي للمناطق). الهدف: MVP حقيقي، نظيف، قابل للتوسع.
